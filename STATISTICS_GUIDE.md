# Руководство по статистике

## Описание

Система автоматически собирает статистику по активности бота в таблицу `daily_stats` в базе данных.

### Что отслеживается:

1. **Дата** - день, за который собрана статистика
2. **Количество сказок** - сколько сказок было создано за день
3. **Новые пользователи** - сколько новых пользователей зарегистрировалось
4. **Команда /start** - сколько раз была нажата команда /start
5. **Заполненные анкеты** - сколько пользователей полностью заполнили анкету (ответили на 3 вопроса)

## Установка

### Шаг 1: Применить миграцию

Запустите файл для применения миграции:

```bash
apply_daily_stats_migration.bat
```

Или вручную:

```bash
venv\Scripts\activate.bat
python apply_daily_stats_migration.py
```

Это создаст таблицу `daily_stats` в базе данных.

### Шаг 2: Перезапустить бота

После применения миграции перезапустите бота:

```bash
restart.bat
```

## Использование

### Просмотр статистики

Для просмотра статистики запустите:

```bash
view_stats.bat
```

Или вручную:

```bash
venv\Scripts\activate.bat
python view_stats.py
```

Это выведет таблицу со статистикой за последние 30 дней и общую сводку.

### Пример вывода:

```
================================================================================
СТАТИСТИКА ПО ДНЯМ
================================================================================

Дата         |   Сказки |    Новые |   /start |   Анкеты
             |          |   польз. |   команд |  заполн
--------------------------------------------------------------------------------
2026-01-20   |       15 |        3 |        5 |        3
2026-01-19   |       22 |        5 |        8 |        4
2026-01-18   |       18 |        2 |        6 |        2
--------------------------------------------------------------------------------

================================================================================
ОБЩАЯ СТАТИСТИКА
================================================================================

Всего дней с данными:        3
Всего создано сказок:        55
Всего новых пользователей:   10
Всего команд /start:         19
Всего заполнено анкет:       9

Средняя активность в день:
  - Сказок:                  18.3
  - Новых пользователей:     3.3
  - Команд /start:           6.3
  - Заполненных анкет:       3.0

================================================================================
```

## Структура таблицы в БД

```sql
CREATE TABLE daily_stats (
    date DATE PRIMARY KEY,
    stories_count INTEGER DEFAULT 0,
    new_users_count INTEGER DEFAULT 0,
    start_command_count INTEGER DEFAULT 0,
    profile_completed_count INTEGER DEFAULT 0,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

## Автоматический сбор

Статистика собирается автоматически при следующих событиях:

1. **Создание сказки** - после успешного сохранения сказки в БД
2. **Новый пользователь** - при первом создании профиля (после ответа на вопросы об имени и возрасте)
3. **Команда /start** - каждый раз при вызове команды /start
4. **Заполнение анкеты** - когда пользователь отвечает на все вопросы анкеты

### 🛡️ Безопасность

Все вызовы сбора статистики обернуты в `try-except` блоки. Это означает:
- ✅ Если сбор статистики упадет с ошибкой, основная функциональность бота продолжит работать
- ✅ Регистрация новых пользователей не прервется из-за проблем со статистикой
- ✅ Создание сказок будет работать, даже если таблица статистики недоступна
- ✅ Все ошибки статистики логируются, но не влияют на пользовательский опыт

## Доступ к данным программно

Вы можете использовать функции из `src/db/repository.py`:

```python
from src.db.repository import get_daily_stats, get_daily_stats_summary, increment_daily_stat

# Получить статистику за последние N дней
stats = get_daily_stats(limit=30)

# Получить общую сводку
summary = get_daily_stats_summary()

# Вручную увеличить счетчик (обычно не требуется)
increment_daily_stat('stories')  # 'stories', 'new_users', 'start_command', 'profile_completed'
```

## Заметки

- Статистика обновляется в реальном времени
- Данные хранятся в UTC
- Каждый день представлен одной строкой в таблице
- При сбое сбора статистики бот продолжит работу (ошибки логируются)

